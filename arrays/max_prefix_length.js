/**
 * @param {string[]} strs
 * @return {string}
 */
// var longestCommonPrefix = function(strs) {
//     const max_length=length(strs[0])
//     for (let i=0;i<length(strs);i++){
//         if strs[i].length<max_length{
//             max_length=strs[i].length
//         }
//     for (let i=0;i<max_length;i++){
    
//     }
    
// }
// };

prefixes=""
var longestCommonPrefix = function(strs) {
    for (let i=0; i<length(strs);i++){
        for (let j=0;j<length(strs[i]);j++){
            
        }
    }
}